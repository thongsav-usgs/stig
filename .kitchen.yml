---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: cida-centos-6.6
    driver-config:
      box: usgs-wsi-centos-6.6
      box_url: ftp://ftpext.usgs.gov/pub/er/wi/middleton/dblodgett/boxes/vmware/opscode_centos-6.6_chef-provisionerless.box

suites:
  - name: default
    run_list:
      - recipe[stig::default]
    attributes:
